#!/bin/sh
echo "🚀 Running pre-push checks..."

# Run quick CI verification
./scripts/ci-verify-quick.sh || {
  echo "❌ Pre-push checks failed!"
  echo "Fix the errors above before pushing."
  exit 1
}

echo "✅ Pre-push checks passed!"
