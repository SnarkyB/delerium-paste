ktor {
  deployment { port = 8080 }
  application { modules = [ AppKt.module ] }
}
storage {
  dbPath = "jdbc:sqlite:/data/pastes.db"
  deletionTokenPepper = ${?DELETION_TOKEN_PEPPER}
  pow { enabled = true, difficulty = 8, ttlSeconds = 180 }
  rateLimit { enabled = false, capacity = 30, refillPerMinute = 30 }
  paste { maxSizeBytes = 1048576, idLength = 10 }
}
